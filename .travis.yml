language: python
dist: xenial
sudo: required


python:
    - "3.6"
    - "3.7"

install:
  # - pip install pandas
  # - pip install selenium
  # - python setup.py install
  - pip install -r requirements.txt

script: python -m pytest
